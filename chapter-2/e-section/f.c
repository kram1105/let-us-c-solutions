#include <stdio.h>
// We have to find out the errors if any

int main() {
    int a, b ; 
    if ( a == 1 & b == 0 ) 
        printf ( "\nGod is Great" ) ;  
}
// Bitwise and is used instead of and operator