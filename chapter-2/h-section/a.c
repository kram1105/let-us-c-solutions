#include <stdio.h>

int main() {
    int tag = 0, code = 1 ; 
    if ( tag == 0 ) 
    ( code > 1 ? printf ( "\nHello" ) ? printf ( "\nHi" ) ) ; 
    else 
    printf ( "\nHello Hi !!" ) ;
}

// The syntax at line 6 is wrong. There should be : in place of second ?