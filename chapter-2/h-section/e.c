#include <stdio.h>

int main() {
    int n = 9 ; 
    ( n == 9 ? printf( "You are correct" ) ;: printf( "You are wrong" ););
}
// there should not be semi colon before colon and before the last closing parentheses