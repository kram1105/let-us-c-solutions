#include <stdio.h>

int main() {
    int i ; 
    for ( i = 1 ; i <= 5 ; printf ( "\n%c", 65 ) ) ; 
        i++ ; 
}
// There is a semi colon at the end of the for loop. So the following loop will be executed infinitely without body and printing A.